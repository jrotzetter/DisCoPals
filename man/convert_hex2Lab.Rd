% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/palette_metrics.R
\name{convert_hex2Lab}
\alias{convert_hex2Lab}
\title{Convert Hex Colors to CIELAB Space}
\usage{
convert_hex2Lab(hex, white_point = "D65")
}
\arguments{
\item{hex}{A \code{character} vector of hex colors (e.g., \code{"#FF0000"}).
Named or unnamed vectors are accepted; if named, row names of the output
will reflect the unique hex values.}

\item{white_point}{A \code{character} string specifying the reference white point
for Lab conversion. Must match standard illuminants accepted by \code{\link[grDevices:convertColor]{grDevices::convertColor()}}.
\code{"D65"} (default) matches sRGB/screen standards.
\code{"D50"} is typically used for print workflows.}
}
\value{
A numeric \code{matrix} with 3 columns:
\describe{
\item{L}{Lightness (0 = black, 100 = white).}
\item{a}{Green (-) to Red (+) opponent channel.}
\item{b}{Blue (-) to Yellow (+) opponent channel.}
}
Row names correspond to the unique input hex codes. Returns an empty
matrix (0 rows) if input is empty.
}
\description{
Transforms a vector of hexadecimal color codes into the device-independent
\strong{CIELAB} color space. This space was intended to be perceptually uniform,
meaning that Euclidean distances between coordinates approximate human
perception of color differences.
}
\details{
Uses \code{\link[grDevices:col2rgb]{grDevices::col2rgb()}} to convert hex to RGB and
\code{\link[grDevices:convertColor]{grDevices::convertColor}} to convert from sRGB to Lab. The input scale is
assumed to be 0-255 (standard 8-bit RGB). The \code{white_point} argument
controls the reference white used in the Lab conversion, allowing for
chromatic adaptation between screen (D65) and print (D50) workflows.
The function automatically deduplicates input colors to optimize performance;
the returned matrix contains only unique colors.
}
\examples{
# Convert a simple vector of colors
example_pal <- c("#FF0000", "#00FF00", "#0000FF")
convert_hex2Lab(example_pal)

# Handle duplicates (returns only unique rows)
colors_dup <- c("#FF0000", "#FF0000", "#000000")
convert_hex2Lab(colors_dup)

}
\seealso{
\code{\link[grDevices:col2rgb]{grDevices::col2rgb()}}, \code{\link[grDevices:convertColor]{grDevices::convertColor()}}
}
