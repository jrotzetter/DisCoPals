% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/palette_visualisation.R
\name{plot_combinations}
\alias{plot_combinations}
\title{Visualize Color Combination Metrics}
\usage{
plot_combinations(combinations, pairs_subset = NULL, separation_threshold = 50)
}
\arguments{
\item{combinations}{A \code{data.frame} containing color comparison metrics, where
each row represents a color pair. This object is typically returned by
\code{\link{analyze_palette}}. It must contain the following columns:
\itemize{
\item \code{Col_1}: The first color in the pair (used for grouping/splitting).
\item \code{Col_2}: The second color in the pair (required for data integrity).
\item \code{contrast_ratio}: Numeric contrast ratio between the colors.
\item \code{deltaE}: Numeric color difference metric.
}
Row names are used as pair identifiers.}

\item{pairs_subset}{Optional \code{character} vector of row names to subset the combinations.
If provided, only these pairs are plotted.}

\item{separation_threshold}{\code{Numeric}. If the number of rows exceeds this value,
the function splits the output into separate plots per unique value in \code{Col_1}.
Default is 50.}
}
\value{
A \code{ggplot} object if a single plot is generated. If the plot is split
(due to \code{separation_threshold}), the function prints multiple plots and
returns \code{NULL} invisibly.
}
\description{
Visualizes color contrast and deltaE metrics for pairs of colors as a grid of
labeled color swatches using \code{ggplot2}. For large datasets, plots can be
split by the first color to improve readability.
}
\examples{
\dontshow{if (.has_packages(c("tibble", "dplyr", "tidyr", "ggplot2", "stringr", "ggtext", "forcats", "colorspace", "spacesXYZ"))) withAutoprint(\{ # examplesIf}
example_pal <- c("#E69F00", "#56B4E9", "#009E73")
combos <- analyze_palette(example_pal, include_background = FALSE)
rownames(combos) <- c("Pair1", "Pair2", "Pair3")

# Plot all combinations
plot_combinations(combos)

# Plot a subset of pairs
plot_combinations(combos, pairs_subset = c("Pair1", "Pair3"))

# Force splitting by first color
plot_combinations(combos, separation_threshold = 2)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link{analyze_palette}} for generating the required input data.
}
