% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/palette_metrics.R
\name{analyze_palette}
\alias{analyze_palette}
\title{Analyze Color Palette Accessibility and Perceptual Differences}
\usage{
analyze_palette(
  pal,
  include_background = TRUE,
  metric = c("2000", "1994", "1976"),
  white_point = "D65"
)
}
\arguments{
\item{pal}{A \code{character} vector of hexadecimal color codes (e.g., \code{c("#FF0000", "#00FF00")}).}

\item{include_background}{\code{Logical}. If \code{TRUE} (default), white (\verb{#FFFFFF}) and
black (\verb{#000000}) are appended to the palette before comparison to evaluate
readability against standard backgrounds.}

\item{metric}{A \code{character} string specifying the DeltaE formula to use.
Must be one of \code{"2000"} (CIEDE2000, most accurate), \code{"1994"} (CIE94),
or \code{"1976"} (CIE76). Defaults to \code{"2000"}.}

\item{white_point}{A \code{character} string specifying the reference white point
for Lab conversion. \code{"D65"} (default) matches sRGB/screen standards.
\code{"D50"} is typically used for print workflows. Passed to \code{\link[=convert_hex2Lab]{convert_hex2Lab()}}.}
}
\value{
A \code{data.frame} with the following columns:
\itemize{
\item \code{Col_1}: Hex code of the first color in the pair.
\item \code{Col_2}: Hex code of the second color in the pair.
\item \code{contrast_ratio}: Numeric contrast ratio between the two colors.
\item \code{deltaE}: Numeric DeltaE value representing perceptual color difference.
}
Returns \code{NULL} if the resulting palette has fewer than 2 colors.
}
\description{
Calculates pairwise color differences for a given palette using contrast ratio
and DeltaE metrics. This function generates all unique pairs of colors in the
input palette and computes perceptual differences and accessibility contrast.
}
\details{
The function relies on the \code{colorspace} package for contrast ratios and
\code{spacesXYZ} for DeltaE calculations. If \code{include_background = TRUE}, standard
black and white backgrounds are added unless they already exist in the palette
(case-insensitive match).

\strong{White Point Selection:}
\itemize{
\item Use \code{"D65"} (default) for digital palettes (web, R figures, UI) as it matches
the native white point of sRGB hex codes.
\item Use \code{"D50"} if comparing against print standards or physical swatches under
standard lighting conditions.
}
}
\examples{
\dontshow{if (.has_packages(c("colorspace", "spacesXYZ"))) withAutoprint(\{ # examplesIf}
# Define a simple palette
example_pal <- c("#E69F00", "#56B4E9", "#009E73")

# Analyze colors with default settings
analyze_palette(example_pal)

# Analyze without adding standard background colors using an older metric
analyze_palette(example_pal, include_background = FALSE, metric = "1976")
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[colorspace:contrast_ratio]{colorspace::contrast_ratio()}}, \code{\link[spacesXYZ:DeltaE]{spacesXYZ::DeltaE()}}
}
